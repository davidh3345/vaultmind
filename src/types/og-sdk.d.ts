// Optional runtime dependency — install @0gfoundation/0g-storage-ts-sdk to enable
// real 0G Storage. Declared loosely so the build succeeds without it present.
declare module "@0gfoundation/0g-storage-ts-sdk";
